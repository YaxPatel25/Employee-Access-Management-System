server:
  port: 8080

spring:
  application:
    name: employee-access-service
  datasource:
    url: jdbc:postgresql://localhost:5432/employee-access-db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true